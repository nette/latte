<?php
%A%
		$iterations = 0;
		foreach ($iterator = $__it = new LR\CachingIterator(['a'], $__it ?? null) as $item) {
			echo '	item
';
			$iterations++;
		}
		if ($iterator->isEmpty()) {
			echo '	empty
';
			$iterations++;
		}
		$iterator = $__it = $__it->getParent();
%A%
