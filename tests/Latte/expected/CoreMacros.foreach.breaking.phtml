<?php
%A%
		$iterations = 0;
		foreach ($iterator = $ʟ_it = new LR\CachingIterator([0, 1, 2, 3], $ʟ_it ?? null) as $item) {
			if ($item % 2) continue;
			echo '	';
			echo LR\Filters::escapeHtmlText($iterator->counter) /* line %d% */;
			echo '. item
';
			$iterations++;
		}
		$iterator = $ʟ_it = $ʟ_it->getParent();
		echo '
---

';
		$iterations = 0;
		foreach ($iterator = $ʟ_it = new LR\CachingIterator([0, 1, 2, 3], $ʟ_it ?? null) as $item) {
			if ($item % 2) {
				$iterator->skipRound();
				continue;
			};
			echo '	';
			echo LR\Filters::escapeHtmlText($iterator->counter) /* line %d% */;
			echo '. item
';
			$iterations++;
		}
		$iterator = $ʟ_it = $ʟ_it->getParent();
		echo '
---

';
		$iterations = 0;
		foreach ($iterator = $ʟ_it = new LR\CachingIterator([0, 1, 2, 3], $ʟ_it ?? null) as $item) {
			if ($item % 2) break;
			echo '	';
			echo LR\Filters::escapeHtmlText($iterator->counter) /* line %d% */;
			echo '. item
';
			$iterations++;
		}
		$iterator = $ʟ_it = $ʟ_it->getParent();
%A%
