<?php
%A%
		$iterations = 0;
		foreach (['a', 'b'] as $item) /* line %d% */ {
			echo '	item
';
			$iterations++;
		}
		echo '
---

';
		$iterations = 0;
		foreach ($iterator = $ʟ_it = new LR\CachingIterator(['a', 'b'], $ʟ_it ?? null) as $item) /* line %d% */ {
			echo '	';
			echo LR\Filters::escapeHtmlText($iterator->counter) /* line %d% */;
			echo "\n";
			$iterations++;
		}
		$iterator = $ʟ_it = $ʟ_it->getParent();
		echo LR\Filters::escapeHtmlText($iterator === null ? 'is null' : null) /* line %d% */;
%A%
